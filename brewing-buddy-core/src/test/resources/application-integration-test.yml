spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/brewing-buddy-db-test
    username: brewing-buddy-test
    password: brewing-buddy-test
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
